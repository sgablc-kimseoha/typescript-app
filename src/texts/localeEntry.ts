import enMessages from './messages/enMessages.json';
import koMessages from './messages/koMessages.json';

const localeEntry: { [key: string]: any } = {
  en: enMessages,
  ko: koMessages,
};

export default localeEntry;
