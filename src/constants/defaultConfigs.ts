import enMessages from 'locales/en-US.json';
import koMessages from 'locales/ko-KR.json';

export const LANGUAGE_LIST: { [key: string]: any } = {
  en: enMessages,
  ko: koMessages,
};
export const DEFAULT_LANGUAGE = 'ko';
export const GLOBAL_LANGUAGE = 'en';
